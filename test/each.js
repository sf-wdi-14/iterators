var expect = require('chai').expect;

describe('#each()'), function() {
	it('returns the entered array')
}
// Write a test for a method called 'each' that loops over an array and
// returns the array.
// Then write the method that will make that test pass.
